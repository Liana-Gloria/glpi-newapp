Login.jsx — champ pré-rempli avec le code par défaut, POST vers middleware, redirige si OK
Import.jsx — 4 zones de dépôt (3 CSV + 1 ZIP), FormData envoyé à POST /api/import, barre de progression
Reset.jsx — bouton de confirmation à deux étapes, appel DELETE /api/reset
Dashboard.jsx — cards de comptage (items par type, tickets par statut), TanStack Query pour le fetch
Tickets.jsx — tableau paginé + fiche ticket en modal ou page dédiée